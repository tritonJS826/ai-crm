// eslint-disable-next-line @typescript-eslint/no-unused-vars
import {ConversationStatus} from "src/models/Conversation";

/**
 * @deprecated Use {@link ConversationStatus} instead.
 */
// Conversation status options.
export enum ConversationStatusOld {
    OPEN = "OPEN",
    CLOSED = "CLOSED",
  }
